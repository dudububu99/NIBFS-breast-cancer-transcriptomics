NIBFS GitHub v1.2.5 — SHORT-PATH PACKAGE

This archive contains the same repository content as the synced-patch release.
Only the outer folder name was shortened to reduce Windows path length.

Recommended extraction:
  C:\NIBFS\

Do NOT extract inside another long Downloads/project folder if Windows long-path
support is disabled.

Scientific code/results were not modified by this repackaging.
